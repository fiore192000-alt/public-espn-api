# Training and evaluation stack (CPU only — no GPU required).
# Install into the same environment that runs Freqtrade, or into a separate
# venv if you train outside Docker.
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
xgboost>=2.0
pyarrow>=14.0
pytest>=7.4
